// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract UserInputArray {
    uint[] public numbers; 


    function setArray(uint[] memory _numbers) public {
        numbers = _numbers;
    }

    // Array return
    function getArray() public view returns (uint[] memory) {
        return numbers;
    }
}
